import * as Actions from '../actions';


export default (state, action) => {
    switch (action.type) {
        case Actions.ALGORITHM_FIRST:
            return state;
        default:
            return state;
    }
};